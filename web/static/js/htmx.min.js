// Placeholder for HTMX. Bundle-time step will replace this file with the
// real https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js. For the MVP the
// UI does not yet require HTMX behaviors — all interactions are plain form
// posts. When we add live features (inline validation, progress, etc.)
// we'll drop the real htmx.min.js in via the Dockerfile.
console.debug("nebula-starcaller: htmx placeholder");
